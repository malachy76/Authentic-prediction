# ⚽ European League Prediction App

This app highlights matches where:
- A team has **5 consecutive wins**
- That team has **odds ≤ 1.50** in their next fixture

Built with **Streamlit** and the [Football-Data.org API](https://www.football-data.org/).

---

## 🚀 Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
